import { jsx } from "@emotion/react";


export function IncomingSeedsView({
    ...props
}: jsx.JSX.IntrinsicElements["div"]) {
    // - incoming seeds / can set priority or trash
    return <div {...props}>
        Incoming Seeds View
    </div>;
}
